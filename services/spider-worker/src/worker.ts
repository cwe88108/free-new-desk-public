import vm from 'node:vm';import { randomUUID } from 'node:crypto';import { createRuntimeShim } from '@free-new-desk/catvod-runtime-shim';
type JsonRecord=Record<string,unknown>;type SpiderExports=Record<string,(...args:unknown[])=>unknown|Promise<unknown>>;interface BrokerPending{resolve:(value:string)=>void;reject:(error:Error)=>void;timer:NodeJS.Timeout;}let spider:SpiderExports={};let sourceId='';let proxyBaseUrl='';let proxyToken='';const storage=new Map<string,string>();const brokerPending=new Map<string,BrokerPending>();const send=(message:unknown)=>{if(process.send)process.send(message);};function brokerFetch(url:string,options:JsonRecord={}):Promise<string>{const id=randomUUID();return new Promise((resolve,reject)=>{const timer=setTimeout(()=>{brokerPending.delete(id);reject(new Error('Broker request timeout'));},15_000);brokerPending.set(id,{resolve,reject,timer});send({type:'broker.request',id,sourceId,url,options});});}
function makeContext(code:string):SpiderExports{const module={exports:{}} as{exports:SpiderExports};const safeConsole={log:(...args:unknown[])=>send({type:'log',level:'info',args:args.map(String).slice(0,8)}),warn:(...args:unknown[])=>send({type:'log',level:'warn',args:args.map(String).slice(0,8)}),error:(...args:unknown[])=>send({type:'log',level:'error',args:args.map(String).slice(0,8)})};const shim=createRuntimeShim({sourceId,store:storage,fetchText:(url,options)=>brokerFetch(url,options as JsonRecord),proxyResolver:(url)=>proxyBaseUrl?`${proxyBaseUrl}/proxy?u=${encodeURIComponent(url)}`:url});const context=vm.createContext({module,exports:module.exports,console:safeConsole,URL,URLSearchParams,setTimeout,clearTimeout,TextEncoder,TextDecoder,...shim});const script=new vm.Script(`"use strict";\n${code}\n;globalThis.__spider=module.exports;`,{filename:`source-${sourceId||'unknown'}.js`});script.runInContext(context,{timeout:2_000});const exported=(context as unknown as{__spider?:SpiderExports}).__spider;if(!exported||typeof exported!=='object')throw new Error('Spider script must assign module.exports');return exported;}
async function call(method:string,args:unknown[]):Promise<unknown>{const fn=spider[method];if(typeof fn!=='function')throw new Error(`Spider method not implemented: ${method}`);return Promise.race([Promise.resolve(fn(...args)),new Promise((_,reject)=>setTimeout(()=>reject(new Error(`Spider method timeout: ${method}`)),30_000))]);}
process.on('message',(message:unknown)=>{void(async()=>{if(!message||typeof message!=='object')return;const data=message as JsonRecord;if(data.type==='broker.response'){const id=String(data.id??'');const pending=brokerPending.get(id);if(!pending)return;clearTimeout(pending.timer);brokerPending.delete(id);if(data.error)pending.reject(new Error(String(data.error)));else pending.resolve(String(data.result??''));return;}const id=String(data.id??'');try{if(data.type==='init'){sourceId=String(data.sourceId??'');proxyBaseUrl=String(data.proxyBaseUrl??'');proxyToken=String(data.proxyToken??'');storage.clear();spider=makeContext(String(data.code??''));if(typeof spider.init==='function')await call('init',[{sourceId}]);send({type:'response',id,result:{ok:true}});return;}if(data.type==='call'){const result=await call(String(data.method??''),Array.isArray(data.args)?data.args:[]);if(result&&typeof result==='object'&&proxyBaseUrl&&typeof(result as JsonRecord).url==='string'&&String((result as JsonRecord).url).startsWith(proxyBaseUrl)){const value=result as JsonRecord;value.headers={...((value.headers&&typeof value.headers==='object')?value.headers as JsonRecord:{}),Authorization:`Bearer ${proxyToken}`};}send({type:'response',id,result});return;}throw new Error('Unsupported worker message');}catch(error){send({type:'response',id,error:error instanceof Error?error.message:String(error)});}})();});process.on('disconnect',()=>process.exit(0));
