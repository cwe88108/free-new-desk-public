import http,{type IncomingMessage,type ServerResponse} from 'node:http';import { randomBytes } from 'node:crypto';export interface ProxyDescriptor{baseUrl:string;token:string;}function mediaPlaylist(type:string,url:string):boolean{return/mpegurl/i.test(type)||/\.m3u8(?:\?|$)/i.test(url);}const enc=(value:string)=>Buffer.from(value,'utf8').toString('base64url');const dec=(value:string)=>Buffer.from(value,'base64url').toString('utf8');function headersFrom(url:URL):Record<string,string>{const raw=url.searchParams.get('h');if(!raw)return{};try{const value=JSON.parse(dec(raw)) as Record<string,unknown>;return Object.fromEntries(Object.entries(value).filter(([,v])=>typeof v==='string') as Array<[string,string]>);}catch{return{};}}function srtToVtt(value:string):string{return`WEBVTT\n\n${value.replace(/^\uFEFF/,'').replace(/(\d{2}:\d{2}:\d{2}),([0-9]{3})/g,'$1.$2')}`;}export class ProxyService{#server:http.Server|undefined;#token='';#baseUrl='';async start():Promise<ProxyDescriptor>{if(this.#server)return{baseUrl:this.#baseUrl,token:this.#token};this.#token=randomBytes(32).toString('base64url');this.#server=http.createServer((request,response)=>{void this.#handle(request,response);});await new Promise<void>((resolve,reject)=>{this.#server?.once('error',reject);this.#server?.listen(0,'127.0.0.1',()=>resolve());});const address=this.#server.address();if(!address||typeof address==='string')throw new Error('Proxy failed to bind TCP port');this.#baseUrl=`http://127.0.0.1:${address.port}`;return{baseUrl:this.#baseUrl,token:this.#token};}url(target:string,headers:Record<string,string>={}):string{if(!this.#baseUrl)throw new Error('Proxy is not running');const h=Object.keys(headers).length?`&h=${encodeURIComponent(enc(JSON.stringify(headers)))}`:'';return`${this.#baseUrl}/proxy?u=${encodeURIComponent(target)}${h}`;}subtitleUrl(target:string,headers:Record<string,string>={}):string{if(!this.#baseUrl)throw new Error('Proxy is not running');const h=Object.keys(headers).length?`&h=${encodeURIComponent(enc(JSON.stringify(headers)))}`:'';return`${this.#baseUrl}/subtitle?u=${encodeURIComponent(target)}${h}`;}stop():Promise<void>{const server=this.#server;this.#server=undefined;this.#baseUrl='';this.#token='';if(!server)return Promise.resolve();return new Promise(resolve=>server.close(()=>resolve()));}#rewriteManifest(body:string,target:string,headers:Record<string,string>):string{const base=new URL(target);return body.split(/\r?\n/).map(line=>{if(!line.trim())return line;if(line.startsWith('#'))return line.replace(/URI="([^"]+)"/g,(_all,uri:string)=>{try{return`URI="${this.url(new URL(uri,base).toString(),headers)}"`;}catch{return`URI="${uri}"`;}});try{return this.url(new URL(line.trim(),base).toString(),headers);}catch{return line;}}).join('\n');}async #fetch(target:string,headers:Record<string,string>):Promise<Response>{return fetch(target,{redirect:'follow',signal:AbortSignal.timeout(20_000),headers:{'user-agent':'FreeNewDesk/1.3',...headers}});}async #handle(request:IncomingMessage,response:ServerResponse):Promise<void>{try{if(request.headers.authorization!==`Bearer ${this.#token}`){response.writeHead(401,{'content-type':'text/plain'});response.end('Unauthorized');return;}const incoming=new URL(request.url??'/',this.#baseUrl||'http://127.0.0.1');if(incoming.pathname!=='/proxy'&&incoming.pathname!=='/subtitle'){response.writeHead(404);response.end();return;}const target=incoming.searchParams.get('u');if(!target||!/^https?:\/\//i.test(target)){response.writeHead(400);response.end('Invalid target');return;}const forwarded=headersFrom(incoming);const upstream=await this.#fetch(target,forwarded);const type=upstream.headers.get('content-type')??'application/octet-stream';if(incoming.pathname==='/subtitle'){const text=await upstream.text();const body=/\.srt(?:\?|$)/i.test(target)||/subrip/i.test(type)?srtToVtt(text):text;response.writeHead(upstream.status,{'content-type':body.startsWith('WEBVTT')?'text/vtt; charset=utf-8':type,'cache-control':'no-store'});response.end(body);return;}if(mediaPlaylist(type,target)){const body=this.#rewriteManifest(await upstream.text(),target,forwarded);response.writeHead(upstream.status,{'content-type':type,'cache-control':'no-store'});response.end(body);return;}const passthrough:Record<string,string>={'content-type':type,'cache-control':'no-store'};const length=upstream.headers.get('content-length');if(length)passthrough['content-length']=length;const range=upstream.headers.get('content-range');if(range)passthrough['content-range']=range;response.writeHead(upstream.status,passthrough);if(upstream.body)for await(const chunk of upstream.body as AsyncIterable<Uint8Array>)response.write(chunk);response.end();}catch(error){response.writeHead(502,{'content-type':'application/json'});response.end(JSON.stringify({error:error instanceof Error?error.message:String(error)}));}}}
