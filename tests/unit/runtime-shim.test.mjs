import test from 'node:test';import assert from 'node:assert/strict';import {createRuntimeShim} from '../../packages/catvod-runtime-shim/dist/index.js';
test('CatVod runtime shim exposes request storage crypto compression and selector APIs',async()=>{const calls=[];const shim=createRuntimeShim({sourceId:'shim-test',fetchText:async(url,options)=>{calls.push({url,options});return '<div class="item">Fixture</div>';},proxyResolver:url=>`proxy:${url}`});assert.match(await shim.$fetch('https://example.test/a'),/Fixture/);assert.match((await shim.req('https://example.test/b',{method:'POST',body:'x'})).content,/Fixture/);assert.equal(calls.length,2);assert.equal(shim.local.set('token','abc'),true);assert.equal(shim.local.get('token'),'abc');assert.equal(await shim.js2Proxy('https://media.test/a.m3u8'),'proxy:https://media.test/a.m3u8');assert.equal(shim.CryptoJS.SHA256('abc').toString(),'ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad');const zipped=shim.pako.gzip('hello');assert.equal(shim.pako.ungzip(zipped,{to:'string'}),'hello');const $=shim.cheerio.load('<div class="item">Fixture</div>');assert.equal($('.item').text(),'Fixture');});
