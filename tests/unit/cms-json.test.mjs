import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';import {parseCmsJson} from '../../packages/source-adapters/dist/index.js';test('parseCmsJson normalizes common vod fields',async()=>{const payload=JSON.parse(await readFile(new URL('../fixtures/cms-json.json',import.meta.url),'utf8'));assert.deepEqual(parseCmsJson(payload),[{id:'101',name:'Fixture Movie',poster:'https://example.invalid/poster.jpg',remark:'HD'}]);});
