import test from 'node:test';import assert from 'node:assert/strict';import {resolveEpgTemplate} from '../../packages/epg-parser/dist/index.js';test('EPG API template expands deterministic date tokens',()=>{const date=new Date(2026,7,28,14,30,0);const value=resolveEpgTemplate('https://epg.test/{yyyy}/{MM}/{dd}/{date}?h={HH}&t={timestamp}',date);assert.match(value,/\/2026\/08\/28\/20260828\?h=14&t=\d+$/);});
