# V1.4.11 剩余问题

更新日期：2026-09-07。本文只记录尚未达到验收门槛的事项；源码存在、测试通过、打包成功和真实设备验收分别记录，不能互相替代。

## 1. 完整测试仍有 11 项无法判定

- 最终 `npm test`：162 项，151 项通过，11 项失败。
- 11 项均依赖测试进程创建的 `127.0.0.1` HTTP 服务，涉及来源审计、JS Spider、Proxy、CMS JSON/XML、WebDAV、声明式规则、RequestBroker 和 Type4。
- 本机独立验证显示：监听端口存在，Node `http.get`、`fetch` 和 Windows `Test-NetConnection` 仍全部 `ETIMEDOUT`。因此不能把这些失败写成代码通过，也不能据此断定 11 个业务模块本身有缺陷。
- WebDAV 实际播放已改为 libmpv 直接访问远端地址并通过命名管道传递请求头，不再依赖本机 HTTP Range 桥；WebDAV 目录扫描仍需真实服务器验收。
- 处理要求：修复 Windows 回环 TCP 后原样重跑完整测试，不删除或放宽这些测试。

## 2. 150% 普通模式出现过加载接收超时

- 六组连续显示矩阵首次结果为 5/6：150% 普通模式两次记录 279.8 ms、270.6 ms，超过 200 ms 的异步接收预算；其他五组通过。
- 系统负载恢复后，150% 普通模式单独原样重测通过。原失败已保留在 `dist/ui-smoke/matrix-summary.json`，不能改写为“六组首次全部通过”。
- 当前判断：功能可用，仍存在连续矩阵负载下的性能抖动。后续应采集 `playerHostStartMs`、`playbackRequestDispatchMs` 和 `playerLoadAcceptedMs` 分段数据后再决定是否调整实现；不建议直接放宽 200 ms 门槛。

## 3. V4 的真实环境验收尚未完成

- 尚未提供真实 WebDAV/SMB 地址和账号，无法验证认证、seek、断网恢复、凭据重启行为及 SMB 会话冲突。
- 尚未提供 MP3、APE、FLAC、WAV 基线样本及可选 ALAC/DSF/DFF/CUE 样本，无法给出格式/编码/位深/采样率兼容矩阵。当前只通过本地生成 PCM WAV 的 PlayerHost 加载、查询、切换 profile、停止和失败恢复测试。
- 尚未完成真实直播长时间播放、直播与点播/音乐六向切换、多窗口、预览与全屏场景的人工验收，因此 R09 不能标记为真实场景闭环。
- 5000 频道索引、EPG 边界和虚拟列表有自动化测试；真实 5000+ 频道页面、真实多显示器 DPI 和 Windows 系统高对比度仍待人工验收。
- 原生 DSD/DoP 需要真实 DAC 和驱动，当前只声明 PCM 回退能力，未声明原生 DSD 已实现。

## 4. 增强方案仍有明确未实现项

- 在线歌词目前只接入 LRCLIB；QQ/网易云的可用性、授权和长期稳定性未形成可发布实现。
- 在线封面目前只接入 iTunes Search；MusicBrainz/CAA、Deezer 等多源回退未实现。
- 在线匹配已有标题、艺人、专辑、版本词拒绝和缓存版本隔离，但候选分差、时长约束及短期负缓存仍未完整实现。
- 音乐曲目列表已改为固定行高、前后缓冲窗口的虚拟滚动；页面已提供扫描中取消控件，并可从上次播放位置恢复。当前仍缺少 1000/10000/50000 首真实曲库的人工交互和内存曲线证据。
- 全屏控制的基础播放、seek、音量、直播换台和退出已实现；其可用性仍需真实视频、真实全屏输入和多显示器验证。

## 5. 打包限制

- 安装版和便携版已生成。构建时将 winCodeSign 工具解压到项目内缓存，保留默认的 Windows 可执行文件资源编辑；未关闭应用或 Chromium 沙箱。
- 本机没有代码签名证书，安装版、便携版和主程序的 Authenticode 状态仍为 `NotSigned`，不能将构建日志中的“signing with signtool.exe”解释为已经完成发行签名。
- 包内已包含 `THIRD_PARTY_NOTICES.md`、`music-metadata@11.15.0` 和实际使用的 LGPL libmpv 来源说明。
