# V1.4.9 剩余问题

## PlayerHost 异步接收偶发超过 200 ms

- **现象**：打包界面回归测试在进入或再次进入播放器页时，曾记录 `227.7 ms`、`272.4 ms`、`204.5 ms`、`268.7 ms` 和 `208.3 ms`，超过现有 `200 ms` 接收预算；同时暴露过原生播放器子窗口间歇性消失。
- **已定位原因**：PlayerHost 冷启动探测粒度过粗；多个界面请求可能并发重复启动 PlayerHost；媒体加载仍使用同步 `mpv_command`；相同 VOD/直播参数和空请求头被重复同步设置。高 DPI 坐标重复缩放和界面测试读取旧窗口标题是另外两项已修复的测试与窗口同步问题。
- **已实施修正**：共享单次 PlayerHost 启动、应用显示后后台预热、使用 `mpv_command_async` 并处理命令失败事件、仅在配置变化时更新加载参数、由渲染进程传递实际像素缩放、测试断言前重新确认当前页面。
- **当前状态**：原生程序与专项测试已通过，最终打包界面矩阵仍需确认在 100%、125% 和 150% 高对比度环境下均稳定满足 `200 ms` 预算。若仍超限，应记录实际分位数并检查 libmpv 属性设置或系统调度延迟，不能直接放宽指标。

## Codex 沙箱内打包启动 smoke 未通过

- **现象**：2026-09-05 在 Codex 沙箱命令环境中启动 `dist/releases/win-unpacked/Free New Desk.exe` 时，标准 packaged smoke 报 `Packaged application exited during startup with code -2147483645`；保留调试目录后日志显示主进程已 ready，但 renderer 进程 `launch-failed`，exitCode 为 `49`。
- **对照**：同一机器同一时段，私有 V1.4.9 包也复现相同 renderer `launch-failed`，公共版仅修改 release URL 与公开仓库文案，因此当前证据不支持把该问题归因于脱敏改动。
- **当前状态**：公共版已完成源码构建、原生 PlayerHost 编译、PlayerHost 集成测试和发行包生成；packaged renderer smoke 仍需在普通用户桌面环境下复核。若普通双击也复现，应优先检查 Electron 44 renderer sandbox 启动条件、系统策略和崩溃转储。
